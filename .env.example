# Porta do servidor (opcional, padrão: 3000)
PORT=3000

# API Key para autenticação (obrigatório)
# Valor em produção: 62ba341e-4a6d-4572-b395-6a99f51010d9
API_KEY=your-api-key-here

# Configuração do PostgreSQL - Opção 1: Variáveis individuais
PGHOST=localhost
PGPORT=5432
PGDATABASE=clients&sales
PGUSER=your-db-user
PGPASSWORD=your-db-password

# Configuração do PostgreSQL - Opção 2: URL completa (sobrescreve as variáveis acima)
# DATABASE_URL=postgresql://user:password@host:port/database

# Ambiente (opcional)
NODE_ENV=production
