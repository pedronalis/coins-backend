# Porta do servidor (opcional, padrão: 3000)
PORT=3000

API_KEY=your-api-key-here

PGHOST=localhost
PGPORT=5432
PGDATABASE=
PGUSER=your-db-user
PGPASSWORD=your-db-password

# Configuração do PostgreSQL - Opção 2: URL completa (sobrescreve as variáveis acima)
# DATABASE_URL=postgresql://user:password@host:port/database

# Ambiente (opcional)
NODE_ENV=production
